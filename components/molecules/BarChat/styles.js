import styled from 'styled-components'

export const ContainerInputText = styled.form`
  background-color: ${({ theme }) => theme.backgroundPrimary};
  display: flex;
  align-items: center;
  justify-content: space-between;
  position: fixed;
  bottom: 0;
  left: 0;
  width: 100%;
  padding: 10px 16px;
  
`
